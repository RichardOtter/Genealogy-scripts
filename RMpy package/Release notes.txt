RMpy package

Version: 1.2.0
to be in suite v1.1.0
New feature release

Testing:
Tested with new environment: procedure and standard test database.

Doc:
new ReadMe

Adds new app configurations-
RMNOCASE_optional
RegExp_optional

Now allows multiline value in config file for the REPORT_FILE_DISPLAY_APP
if if is a single line variable, no chnage from before, if a second line
is present, the text on the second line is used as a argument on the command
line between the app name and the file name.

Code improvements:
minor change in import order
Launcher now sets the current directory to the location of the calling script file.
No longer calls code to deal with frozen code distributions.

=========================================================================DIV80==
Version: 1.1.0
released in suite v1.0.2


